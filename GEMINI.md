# Project Instructions

## Rules

- When finishing a task, ALWAYS run `npm run build` to ensure the project integrity.
- Never add "Co-Authored-By" or AI attribution to commits. Use conventional commits only.
- Always use surgical replacements to avoid regressions.
- Ensure all queries are filtered by `clinica_id`.
